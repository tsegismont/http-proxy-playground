kind: Service
apiVersion: v1
metadata:
  name: order-service
  namespace: default
  labels:
    k8s-app: order-service
spec:
  type: ClusterIP
  selector:
    k8s-app: order-server
  ports:
    - protocol: TCP
      port: 8443
      targetPort: 8443
